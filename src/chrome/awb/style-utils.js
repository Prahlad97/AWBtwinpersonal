/** Vendored from production `styles/style-utils.js`. */
export const fontStyling = (family, size, weight, lineHeight) => ({
  fontFamily: family,
  fontSize: size,
  fontWeight: weight,
  lineHeight: lineHeight,
});
